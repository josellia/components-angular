import { HighdarkDirective } from './highdark.directive';

describe('HighdarkDirective', () => {
  it('should create an instance', () => {
    const directive = new HighdarkDirective();
    expect(directive).toBeTruthy();
  });
});
